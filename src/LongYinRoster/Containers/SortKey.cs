namespace LongYinRoster.Containers;

/// <summary>
/// v0.7.2 — ContainerPanel 정렬 키. spec §3.3 (사용자 확정 4 키).
/// </summary>
public enum SortKey
{
    Category = 0,
    Name     = 1,
    Grade    = 2,
    Quality  = 3,
}
