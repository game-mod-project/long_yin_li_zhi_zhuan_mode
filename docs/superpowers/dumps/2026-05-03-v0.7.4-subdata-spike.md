# v0.7.4 D-1 — sub-data wrapper 필드 inventory spike

**일시**: 2026-05-03
**dump 경로**: BepInEx/LogOutput.log 의 `[v0.7.4 spike]` 줄
**샘플**: 인벤 156 items 중 카테고리별 첫 1+ — 6/6 카테고리 모두 cover ✓
**baseline**: ProbeSubData (commit `ddc4eb8`) → F12 trigger

## ItemType enum 매핑 확인

`ItemType` enum (game-internal):

| Type 값 | enum name | 카테고리 | sample |
|---|---|---|---|
| 0 | `Equip` | 장비 | 절세大剑 (idx=26) |
| 2 | `Food` | 음식 (subType≥1) / 단약 (subType=0)? | 十全大补汤 (idx=8) |
| 3 | `Book` | 비급 | 多情飞刀 (idx=0) |
| 4 | `Treasure` | 보물 | 紫檀琵琶 (idx=22) |
| 5 | `Material` | 재료 | 절세食材 (idx=14) |
| 6 | `Horse` | 말 | 神凫马 (idx=5) |

**type=1 미발견** — v0.7.0+ ItemCategoryFilter 의 `Other = 99` 로 커버.

## ItemData top-level 필드 (sample 6 items 합집합)

| 필드 | 타입 | sample 값 | v0.7.2 spike 등재 | 한글 라벨 후보 |
|---|---|---|---|---|
| `isWrapped` | Boolean (Field) | False | meta — 필터 | — |
| `pooledPtr` | IntPtr (Field) | 1535223784864 | meta — 필터 | — |
| `type` | ItemType (Property) | Book/Horse/Food/Material/Treasure/Equip | ✓ | 종류 |
| `name` | String | 多情飞刀 | ✓ | 이름 |
| **`itemID`** | **Int32** | **0/45/5/0/0/10** | ❌ **신규 발견** | item ID (게임 내부) |
| `subType` | Int32 | 0/3 | ✓ | 서브타입 |
| `checkName` | String | null/无 | ✓ | (게임 내부 검증용 — 표시 X) |
| `describe` | String | null/"전설에 따르면..." | ✓ | 설명 (있을 때만) |
| `value` | Int32 | 32000/12800/... | ✓ | 가격 |
| `itemLv` | Int32 (0..5) | 5 (전부 최대) | ✓ | 등급 |
| `rareLv` | Int32 (0..5) | 5 (전부 최대) | ✓ | 품질 |
| `weight` | Single | 6/30/3/6/12/24 | ✓ | 무게 |
| `isNew` | Boolean | False | ✓ | 신품 표시 |
| `poisonNum` | Single | 0 | ✓ | 독 수치 |
| `poisonNumDetected` | Boolean | False | ✓ | 독 감지 여부 |
| `setName` | String | null | ✓ | 세트 이름 |
| `equipmentData` | EquipmentData | null/EquipmentData | ✓ | (sub-data wrapper) |
| `medFoodData` | MedFoodData | null/MedFoodData | ✓ | (sub-data wrapper) |
| `bookData` | BookData | null/BookData | ✓ | (sub-data wrapper) |
| `treasureData` | TreasureData | null/TreasureData | ✓ | (sub-data wrapper) |
| `materialData` | MaterialData | null/MaterialData | ✓ | (sub-data wrapper) |
| `horseData` | HorseData | null/HorseData | ✓ | (sub-data wrapper) |
| `ObjectClass` | IntPtr (Property) | 1530348164768 | meta — 필터 | — |
| `Pointer` | IntPtr (Property) | 1535223784864 | meta — 필터 | — |
| `WasCollected` | Boolean | False | meta — 필터 | — |

## 카테고리별 sub-data wrapper 필드

### type=3 비급 (`bookData`) — sample: `多情飞刀`

| 필드 | 타입 | sample 값 | 한글 라벨 후보 |
|---|---|---|---|
| `skillID` | Int32 | 287 | 무공 ID |

> **plan 가설 `learnLv`/`maxLearnLv` 는 ❌ 미존재** — bookData 는 `skillID` 단일 필드. 학습 진척도는 game 의 다른 path (HeroData.kungfuSkills 등) 에 있고 bookData 자체는 ID 연결만.

### type=6 말 (`horseData`) — sample: `神凫马`

| 필드 | 타입 | sample 값 | 한글 라벨 후보 |
|---|---|---|---|
| `equiped` | Boolean | False | 착용중 |
| `speed` | Single | 109 | 속도 (base) |
| `power` | Single | 56 | 힘 (base) |
| `sprint` | Single | 86 | 스프린트 (base) |
| `resist` | Single | 99 | 인내 (base) |
| `speedAdd` | Single | 0 | 속도 강화 |
| `powerAdd` | Single | 0 | 힘 강화 |
| `sprintAdd` | Single | 0 | 스프린트 강화 |
| `resistAdd` | Single | 0 | 인내 강화 |
| `maxWeightAdd` | Single | 0 | 최대무게 추가 |
| `nowPower` | Single | 109 | 현재 power |
| `favorRate` | Single | 1 | 호감 율 |
| `sprintTimeLeft` | Single | 0 | 스프린트 남은시간 |
| `sprintTimeCd` | Single | 0 | 스프린트 쿨타임 |

> 가장 풍부한 sub-data wrapper. 14 필드 모두 의미 있음 (정확한 한글 라벨은 게임 UI 와 cross-reference 필요).

### type=2 음식·단약 (`medFoodData`) — sample: `十全大补汤` (음식, subType=0)

| 필드 | 타입 | sample 값 | 한글 라벨 후보 |
|---|---|---|---|
| `enhanceLv` | Int32 | 0 | 강화 lv |
| `changeHeroState` | ChangeHeroStateData | (객체) | 영웅 상태 변경 (추가 reflection) |
| `randomSpeAddValue` | Int32 | 0 | 무작위 추가 보정 |
| `extraAddData` | HeroSpeAddData | (객체) | 추가 능력치 보정 (추가 reflection) |

> **plan 가설 `effectValue` 는 ❌ 미존재** — 효과는 `changeHeroState` (state 변경) + `extraAddData` (영구 능력치 변경) 두 객체에 분산. v0.7.4 첫 release 는 `enhanceLv` + `randomSpeAddValue` 단순 표시. 객체 fields 는 v0.7.4.x patch 에서 nested reflection.

### type=5 재료 (`materialData`) — sample: `절세食材`

| 필드 | 타입 | sample 값 | 한글 라벨 후보 |
|---|---|---|---|
| `extraAddData` | HeroSpeAddData | (객체) | 추가 능력치 보정 (추가 reflection) |

> 단일 wrapper 필드 — extraAddData 객체 하나. 이름·등급·품질·가격이 핵심 정보.

### type=4 보물 (`treasureData`) — sample: `紫檀琵琶`

| 필드 | 타입 | sample 값 | 한글 라벨 후보 |
|---|---|---|---|
| `fullIdentified` | Boolean | True | 완전 감정 |
| `identifyKnowledgeNeed` | Single | 101 | 감정 필요 지식 |
| `treasureLv` | List\<Int32\> | (List) | 보물 lv 목록 |
| `identifyDifficulty` | List\<Single\> | (List) | 감정 난이도 목록 |
| `identified` | List\<Boolean\> | (List) | 감정 여부 목록 |
| `playerGuessTreasureLv` | List\<List\<Int32\>\> | (Nested List) | 플레이어 추측 lv (nested L1) |

> List<List<int>> nested 는 v0.6.3 의 generic JSON deserializer 가 이미 처리. v0.7.4 raw section 에서 1차 표시: `List<List<int>> playerGuessTreasureLv: [count=N]`.

### type=0 장비 (`equipmentData`) — sample: `절세大剑`

| 필드 | 타입 | sample 값 | 한글 라벨 후보 |
|---|---|---|---|
| `enhanceLv` | Int32 | 10 | 강화 lv |
| `littleType` | Int32 | 1 | 세분 타입 (무기 종류 등) |
| `attriType` | Int32 | 4 | 속성 타입 |
| `baseAddData` | HeroSpeAddData | (객체) | 기본 능력치 보정 |
| `extraAddData` | HeroSpeAddData | (객체) | 추가 능력치 보정 |
| `equiped` | Boolean | True | 착용중 |
| `animName` | String | "skill1_2" | 애니메이션 (게임 내부) |
| `equipPoisonData` | EquipPoisonData | (객체) | 독 정보 |
| `speEnhanceLv` | Int32 | 12 | 특수 강화 lv |
| `speWeightLv` | Int32 | 0 | 특수 무게 lv |

> 가장 복잡한 wrapper (10 필드, 4 객체 nested). v0.7.4 첫 release curated: enhanceLv / 착용중 / speEnhanceLv / speWeightLv 4 단순 필드 + value/weight (item top-level). attriType/littleType 의 한글 매핑은 별도 dictionary 필요 (v0.7.4.x 후속).

## ItemDetailReflector 갱신 (Plan §4.5 / Task 3 fix)

Plan 의 placeholder fields 를 spike 결과로 정정:

### Plan 가설 vs 실제 spike 결과

| Plan 가설 | 실제 (spike) | 정정 |
|---|---|---|
| bookData.learnLv / maxLearnLv | bookData.skillID | curated: `무공 ID: {skillID}` 단일 표시 |
| medFoodData.effectValue | medFoodData.enhanceLv / randomSpeAddValue / changeHeroState / extraAddData | curated: 강화/무작위 보정 2 단순 + 객체 2 raw 위임 |
| equipmentData.enhanceLv / equiped (검증) | + 8 필드 (littleType/attriType/animName/speEnhanceLv/speWeightLv 등) | curated 확장 가능 |

### Task 3 의 fake test 필드명 정정

`ItemDetailReflectorCuratedTests.cs` 의 fake item 필드명 plan 작성 시 가설 (실제와 다름):
- `FakeBookData.learnLv` / `maxLearnLv` → `FakeBookData.skillID` (Int32)
- `FakeMedFoodData.effectValue` → `FakeMedFoodData.enhanceLv` (Int32) + `randomSpeAddValue` (Int32)
- `FakeEquipmentData` 그대로 (enhanceLv / equiped 검증됨)

### v0.7.4 첫 release curated 매핑 (제안)

```csharp
// type=0 장비
private static List<(string,string)> GetEquipmentDetails(object item)
{
    var result = new List<(string,string)>();
    var ed = ReadObj(item, "equipmentData");
    if (ed == null) return result;

    int enh = ReadInt(ed, "enhanceLv");
    if (enh > 0) result.Add(("강화", $"+{enh}"));
    result.Add(("착용중", ReadBool(ed, "equiped") ? "예" : "아니오"));
    int speEnh = ReadInt(ed, "speEnhanceLv");
    if (speEnh > 0) result.Add(("특수 강화", $"+{speEnh}"));
    int speWt = ReadInt(ed, "speWeightLv");
    if (speWt > 0) result.Add(("무게 경감", $"+{speWt}"));
    result.Add(("무게", $"{ReadFloat(item, "weight"):F1} kg"));
    result.Add(("가격", ReadInt(item, "value").ToString()));
    return result;
}

// type=3 비급
private static List<(string,string)> GetBookDetails(object item)
{
    var result = new List<(string,string)>();
    var bd = ReadObj(item, "bookData");
    if (bd != null)
    {
        int skillId = ReadInt(bd, "skillID");
        result.Add(("무공 ID", skillId.ToString()));
    }
    result.Add(("무게", $"{ReadFloat(item, "weight"):F1} kg"));
    result.Add(("가격", ReadInt(item, "value").ToString()));
    return result;
}

// type=2 단약·음식
private static List<(string,string)> GetMedFoodDetails(object item)
{
    var result = new List<(string,string)>();
    var mf = ReadObj(item, "medFoodData");
    if (mf != null)
    {
        int enh = ReadInt(mf, "enhanceLv");
        if (enh > 0) result.Add(("강화", $"+{enh}"));
        int randAdd = ReadInt(mf, "randomSpeAddValue");
        if (randAdd > 0) result.Add(("추가 보정", randAdd.ToString()));
        // changeHeroState / extraAddData 는 nested 객체 — v0.7.4.x 후속
    }
    result.Add(("무게", $"{ReadFloat(item, "weight"):F1} kg"));
    result.Add(("가격", ReadInt(item, "value").ToString()));
    return result;
}
```

## 후속 sub-project 자산

- **v0.7.4.x patch 후보**: 나머지 3 카테고리 curated (음식 별도, 보물, 재료). 단 horseData 는 14 필드라 patch 우선순위 ↑ — 사용자 빈도 ↑
- **v0.7.7 (Item editor) 자산**: setter 검증 필요 필드 list — `enhanceLv` / `equiped` (장비/말) / `speEnhanceLv` / `speWeightLv` / `skillID` (비급) / `fullIdentified` (보물). IL2CPP setter strip 위험 v0.5+ PoC FAIL 패턴 재현 가능
- **HeroSpeAddData / ChangeHeroStateData / EquipPoisonData / EquipmentSet (TBD)** 는 nested 객체 — 추가 spike 필요시 v0.7.4.x 또는 v0.7.7 시점에 별도 dump

## 한계 / 미발견

- 샘플이 모두 itemLv=5 / rareLv=5 (최대치) — 다른 grade/quality 의 변화 미확인 (단, 본 spike 의 목적은 필드명 inventory — 값 변화는 v0.7.4 본 release 가 reflection 으로 cover)
- subType=0 음식 (`十全大补汤`) sample — subType≥1 단약 미발견 (사용자 인벤에 단약 ItemCategory.Medicine 이 없는지 또는 분류 다름). v0.7.4 첫 release 는 type=2 단일 매핑 — subType 분기는 v0.7.5 한글화 시점에 ItemCategoryFilter 와 함께 정밀화 검토
- 장비 1 sample (검) — 다른 subType 장비 (갑/투구/신발/장신구) 의 littleType 매핑 미확인 (v0.7.4 release 는 raw 표시, 매핑은 후속)
- HeroSpeAddData 등 nested 객체의 내부 필드 미spike — v0.7.4 raw section 1차는 객체 type 만 표시 (`HeroSpeAddData baseAddData = HeroSpeAddData`)

## release 직전 (Task 9) 복원

- ModWindow.cs:735 의 F12 trigger 를 `ProbeSubData.Run()` → `ProbeRunner.Trigger()` 복원
- `Core/Probes/ProbeSubData.cs` 파일 삭제 (또는 다음 spike 까지 보존 결정 — Task 9 시점 결정)
