# v0.5.2 Smoke 결과 (2026-05-02)

**Build**: v0.5.2 branch HEAD (KungfuListApplier 본문 + PinpointPatcher step 순서 + UI 10 카테고리)
**Tests**: 57/57 PASS

## 알고리즘 요약

- **Clear**: `HeroData.LoseAllSkill()`
- **Wrapper ctor**: `new KungfuSkillLvData(int _skillID)` (reflection)
- **Property setter**: `lv / fightExp / bookExp` reflection set
- **Add**: `HeroData.GetSkill(KungfuSkillLvData wrapper, false, false)`
- **Step 순서**: `RebuildKungfuSkills` 가 `SetActiveKungfu` 직전 — list 정확화 후 active 매칭 (v0.5.1 N7 자동 해소)

---

## 시나리오 1 — 다른 캐릭터 무공 set Apply

**Pre**:
- 다른 캐릭터 (강력한 무공 set 있는 NPC 또는 다른 SaveSlot 의 캐릭터) → mod slot 1 capture
- 현재 캐릭터 (다른 무공 list) load

**실행**: mod F11 → slot 1 → ✓ KungfuList + ✓ ActiveKungfu → ▼ 덮어쓰기

**예상 결과**:
- 무공 list = slot 1 의 set
- active = slot 1 의 active set (missing=0 — list 가 정확해서)
- 스탯 변화 (전투력 등)

**BepInEx 로그 예상**:
```
KungfuList clear (LoseAllSkill): 130 → 0
KungfuList Apply done — removed=130 added=N failed=0
ActiveKungfu Apply done — unequip=0 equip=11 missing=0
```

**결과**: [TBD]

---

## 시나리오 2 — Self-Apply (lv / fightExp 복원)

**Pre**:
- 현재 캐릭터 SaveSlot N → mod slot 1 capture
- 게임에서 무공 일부 변경 (새 무공 학습 또는 기존 무공 lv up)

**실행**: mod F11 → slot 1 → ✓ KungfuList → ▼ Apply (자동백업 → slot 0)

**예상 결과**:
- list = slot 1 시점의 진도 (lv / fightExp / bookExp)

**결과**: [TBD]

---

## 시나리오 3 — Restore (slot 0 자동백업)

**Pre**: 시나리오 2 의 step 2 직후 (slot 0 = 변경된 list)

**실행**: mod slot 0 → ↶ 복원

**예상 결과**: list = 변경된 시점 (자동백업) 으로 복귀

**결과**: [TBD]

---

## 회귀 시나리오 (v0.5.1 baseline 동작 유지)

- v0.5.1 active 동작 — KungfuList off + ActiveKungfu on
- 천부 / 정체성 / 스탯 / 명예 / 스킨 / 자기집 동작
- 외형 / 인벤토리 / 창고 disabled 표시
- legacy 슬롯 (v0.3/v0.4/v0.5.1) 호환

**결과**: [TBD]

---

## Algorithm 수정 history

| 시도 | 알고리즘 | 결과 |
|---|---|---|
| 1차 | LoseAllSkill clear + ctor(int) wrapper + GetSkill add 1회 | 첫 시도 silent fail (130→1만 등록), 두 번째 시도에서 PASS |
| 2차 (final) | + Add phase 2-pass retry (read-back 검증, idempotent) | 한 번의 Apply 로 작동 PASS |

**Side fix** (v0.5/v0.5.2 잠재 버그): SlotFile.Write 의 `appearance` + `kungfuList` JSON 직렬화 누락 — Apply 직전 토글 디스크 저장 작동.

## 종합 판정

✅ **v0.5.2 Smoke PASS** (사용자 보고: "한 번의 Apply 로 작동확인"). Phase 5 (Release) 진행.
