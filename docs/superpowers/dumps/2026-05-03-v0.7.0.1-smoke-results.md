# v0.7.0.1 Smoke Results — 사용자 인게임 검증 PASS

**일시**: 2026-05-03
**Build**: commit `0d25e14`
**검증자**: 사용자 (deepestdark@gmail.com)

## §5.1 #2 ContainerPanel 높이 부족 — PASS

- 창 높이 600 → 760 적용
- 좌측 column ScrollView 동작 확인
- [↔이동] / [↔복사] 버튼 모두 표시
- (Phase 1+2 build 배포 후 사용자 인게임 확인)

## §5.2 #3 ContainerPanel cascade error — PASS

- DrawToast no-op + Toast → ToastService.Push 위임 → BepInEx 로그 폭주 사라짐
- 컨테이너 비어있는 상태에서 [→이동] 시도 → "컨테이너 미선택" toast 표시 + UI 유지 (이전엔 UI 전체 사라졌다가 재표시 회귀)
- [신규] 컨테이너 생성 → 인벤토리 → 컨테이너 [→이동] / [→복사] 정상 동작
- 컨테이너 → 인벤토리/창고 [←이동] / [←복사] 정상 동작
- (Phase 2 iteration 2 build fix 후 PASS, dll md5 `15b626e5...` 부터 정상)

## §5.3 #1 천부 휴식 회귀 — PASS

- Apply 후 9~10 천부 표시 → 휴식 1회 → **천부 9~10개 모두 유지** (이전엔 모두 사라지고 "정력충패" 1개만 남음)
- BepInEx 로그 확인 (Phase 4 iteration 1):
  ```
  RestKeepHeroTagPatch.Prefix: snapshot 17 tag(s)
  RestKeepHeroTagPatch.Postfix: restored 17 tag(s) (was 0, snapshot 17)
  ```
  → ManageTagTime 의 wipe 차단 동작 확인.
- BepInEx 로그 확인 (Phase 4 iteration 2 — root cause 확정 후 fix):
  ```
  RestKeepHeroTagPatch: HeroData.ManageTagTime patched
  RestKeepHeroTagPatch: HeroData.ClearAllTempTag patched
  ... (Apply 진행 — ApplyInProgress=true 라 ClearAllTempTag patch 통과) ...
  ... (휴식) ...
  RestKeepHeroTagPatch.Prefix: snapshot N → Postfix: restored N (ManageTagTime)
  RestKeepHeroTagPatch.Prefix_ClearAllTempTag: snapshot N → Postfix: restored N (CAT)
  ```
- 사용자 PASS 보고: "PASS: 천부 유지"

## Diagnostic 단계 — Root cause 확정 trace

Phase 4 iteration 1 의 ManageTagTime 단독 patch 로는 첫 wipe 만 차단됐으나 후속 wipe 가 발생. iteration 2 에서 diagnostic Postfix 로 추적:

```
[DIAG] RefreshMaxAttriAndSkill (player) — heroTagData.Count=17
[DIAG] ClearAllTempTag (player) — heroTagData.Count=0     ← culprit
```

→ ClearAllTempTag 가 두 번째 wipe path 임을 확정. iteration 2 fix 가 이를 차단.

## 4 UX 이슈 (defer to v0.7.1)

사용자 보고 (smoke 도중 발견된 enhancement):
1. 컨테이너 → 인벤토리/창고 이동/복사 destination 구분 불명확
2. 인벤토리/창고 최대/현재 용량 표시 부재
3. 용량 초과 시 컨테이너 → 게임 이동/복사 가드 부재
4. (HANDOFF #1 sub-project 의 Item 상세 / 아이콘 그리드 / 검색·정렬)

→ `docs/superpowers/dumps/2026-05-03-v0.7.1-container-ux-backlog.md` 에 별도 기록. v0.7.0.1 hotfix scope 외.

## 결론

3/3 PASS. v0.7.0.1 release packaging (Phase 6) 진행 가능.
