# v0.5 PoC 종합 report (2026-05-01) — **양쪽 FAIL — release 안 함**

## TL;DR

v0.5 PoC dual-track (외형 + 무공 active) 실행 결과 양쪽 모두 보수적 FAIL. v0.5.0 release 안 함, main 은 v0.4.0 baseline 유지. 그러나 PoC 단계에서 v0.6 production 작업에 결정적 evidence 발견.

## 결과 매트릭스

| Track | 결과 | 사유 |
|---|---|---|
| 외형 (portraitID + gender) | ❌ G1 FAIL | `portraitID` field 미존재 + 진짜 외형 = `faceData / partPosture` sub-data wrapper graph |
| active (kungfuSkills) | ❌ G3 보수적 FAIL | Method path 작동 (read-back PASS) but 게임 UI 미반영 + save→reload 미검증 |

## 외형 PoC 상세

### Phase 1 dump (skip — option C 채택)
T6 skipped per user. 외형 patterns 후보 method 직접 시도.

### Phase 2 — 1차 시도
- 가설: `portraitID` (int) + `gender` (int) setter direct + 8 후보 refresh method
- 결과: **모두 not found** — HeroData 에 `portraitID / gender` field 자체가 부재

### Phase 2 — Discovery pivot
- Probe 가 자가 enumerate (portrait/face/avatar/head/icon/pic 패턴)
- 발견된 외형 영역:
  - `faceData` (HeroFaceData wrapper) — sub-data graph
  - `partPosture` (PartPostureData wrapper) — sub-data graph
  - `skinID / skinLv / defaultSkinID / setSkinID / playerSetSkin` — 의상 (이미 v0.3 Cat_Skin)
  - `skinColorDark` (float) — 피부색
  - `HeroIconDirty / heroIconDirtyCount` — refresh trigger flag
- 후보 zero-arg refresh method 13개 모두 not found
- **결론**: 진짜 외형은 sub-data wrapper graph — v0.4 PoC A4 (ItemData) 와 동일 미해결 패턴

→ G1 FAIL. v0.6 sub-data graph 통합 작업과 묶음.

## active PoC 상세

### Phase A (save-diff)
- 시나리오: SaveSlot1 (active=X) vs SaveSlot2 (active=Y) JSON diff
- 결정적 발견: **`kungfuSkills[i].equiped` 가 source-of-truth** (NOT `nowActiveSkill`)
- v0.4 A3 PoC FAIL 진짜 원인 = 잘못된 source field
- → G2 PASS

### Phase B (Harmony trace)
- 후보 method 패턴 (Equip/Active/Switch/Toggle) — Hero/Kungfu/Skill/Player 타입 scan
- 5개 patched: `EquipSkill`, `UnequipSkill`, `SetNowActiveSkill`, `KungfuSkillLvData.get_equiped/set_equiped`
- TRACE 결과: 사용자가 active 변경 시 **`UnequipSkill(wrapper, true) × 11` → `EquipSkill(wrapper, true) × 11`** 시퀀스
- 11-슬롯 active array swap 패턴
- → method path 발견 PASS

### Phase C (in-memory toggle)
- 1차: ID 매칭 의존 — 모든 wrapper.kungfuID 가 -1 fallback (KungfuSkillLvData 에 kungfuID field 부재) → 후보 매칭 실패
- 2차: idx 매칭 + DumpWrapperShape — KungfuSkillLvData 의 진짜 ID 필드 = **`skillID`** (NOT kungfuID)
- swap 결과: `UnequipSkill OK + EquipSkill OK + read-back equiped flag 변경 확인` → SUCCESS

### G3 보수적 FAIL 사유
- 게임 UI 무공 패널 변경 안 보임 (사용자 보고)
- save→reload persistence 미검증 (사용자가 직접 비운 시나리오만 시도)
- v0.4 외형 PoC 와 동일 패턴: "method 작동했지만 cache/UI 반영 안 됨"

## 핵심 발견 (v0.6 production code 자산)

### 외형 영역
- 진짜 외형 = `HeroData.faceData (HeroFaceData)` + `HeroData.partPosture (PartPostureData)` sub-data wrapper
- `HeroIconDirty` / `heroIconDirtyCount` — refresh trigger 후보
- `SetSkin(int, int)` / `SetSkeletonGraphicFaceSlot(SkeletonGraphic, int, int)` 등 method signature

### active 영역
- Source-of-truth: `kungfuSkills[i].equiped` (NOT `nowActiveSkill`)
- ID field: `KungfuSkillLvData.skillID` (NOT `kungfuID`)
- Method path: `HeroData.EquipSkill(KungfuSkillLvData, bool=true)` + `UnequipSkill` 동상
- 11-슬롯 array swap 패턴 — game-self 가 full swap 시 11회 Unequip + 11회 Equip
- UI refresh trigger 후보: `KungfuSkillLvData.skillIconDirty / maxManaChanged`, `HeroData.HeroIconDirty`
- Sub-data: `HeroSpeAddData speEquipData / speUseData / extraAddData` (effect 데이터)

## v0.6 통합 작업 후보

v0.5 의 두 FAIL 모두 **sub-data wrapper graph + UI cache invalidation** 으로 귀결:

| 항목 | v0.5 진척 | v0.6 작업 |
|---|---|---|
| 외형 | sub-data graph 가설 확정 | HeroFaceData / PartPostureData wrapper 처리 + sprite refresh |
| active | method path 발견 + read-back PASS | UI refresh path 발견 + save→reload persistence 검증 |
| 인벤토리 (v0.4 A4) | sub-data graph 미해결 | ItemData wrapper graph 처리 |
| 창고 (v0.4 A4) | 동상 | selfStorage.allItem (ItemData[]) 동상 |
| 무공 list (KungfuSkillLvData) | wrapper shape 발견 (skillID, lv 등) | 추가 entry add method 발견 |

→ v0.6 핵심 작업 = **IL2CPP wrapper graph 통합 + game UI refresh path discovery**.

## v0.5 commit history (main)

```
b5031da poc(v0.5): 외형 PoC FAIL — discovery dump + G1 결정
5bf970c poc(v0.5): active Phase A save-diff 결과 — G2 PASS
3fb0d60 feat(probes): ProbeActiveKungfuV2.RunPhaseB body — Harmony trace
99c44cc chore(probes): ProbeRunner default Mode → ActiveTrace
f15536e poc(v0.5): active Phase B trace 결과 + ProbeRunner Mode → ActiveInMemory
d4532c9 feat(probes): ProbeActiveKungfuV2.RunPhaseC body — in-memory toggle
4a8aa24 fix(probes): PhaseC — idx-based 매칭 + DumpWrapperShape
+ Phase 1 foundation commits (T1-T5)
```

## 결정

- ❌ **v0.5.0 release 안 함** — 양쪽 PoC FAIL
- ✅ **PoC artifact 보존** (dumps/ markdown 5 개) — v0.6 의 결정적 evidence
- ✅ **Probe 코드 cleanup** (D16 패턴) — main 에서 임시 코드 제거
- 🔄 **HANDOFF.md 업데이트** — v0.5 PoC report 섹션 + v0.6 후보 갱신
- 📌 **v0.4.0 main baseline 유지** — release tag 추가 안 함, dist zip 추가 안 함

## v0.6 후보 list (HANDOFF §6 갱신용)

1. **sub-data wrapper graph** — 외형 (faceData/partPosture) + 인벤토리 (ItemData) + 창고 통합
2. **active full integration** — method path + UI refresh + save persistence
3. **무공 list** — KungfuSkillLvData wrapper add method
4. **UI cache invalidation discovery** — 외형/active 의 진짜 refresh path
