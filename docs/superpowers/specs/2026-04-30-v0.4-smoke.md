# v0.4 Smoke Checklist

게임 안 검증 결과 기록. 각 항목 PASS / FAIL / N/A 마킹 후 결과 노트 추가.

---

**Build**: `8f3edf1` + followup `10069fd` (C14 base + window/toast fix)
**Capabilities (PoC 결과)**: Identity=true, ActiveKungfu=false, ItemList=false, SelfStorage=false

---

## A. 체크박스 default 검증

- [ ] 모드 창 열기 → 새 슬롯 캡처 (`[+]`)
- [ ] 새 슬롯 선택 → SlotDetailPanel 의 체크박스 상태 확인:
  - [ ] **on** (4): 스탯 / 명예 / 천부 / 스킨
  - [ ] **off** (5): 자기집 add / 정체성 / 무공 active / 인벤토리 / 창고
- [ ] **disabled** (PoC FAIL — 회색 + "(v0.5+ 후보)" suffix): 무공 active / 인벤토리 / 창고
- [ ] **enabled but off** (default): 자기집 add / 정체성

**기대**: V03Default 4개 on + 정체성/자기집 add 토글 가능 + 무공 active/인벤토리/창고 disabled.

**결과**:

---

## B. Toggle 시 즉시 저장

- [ ] 임의 슬롯의 정체성 체크박스 클릭 → 체크됨
- [ ] 모드 창 닫기 (F11) → 다시 열기 (F11) → 같은 슬롯 → 정체성 그대로 체크됨
- [ ] 슬롯 file 직접 확인:
  ```bash
  python -c "import json; d=json.load(open('E:/Games/龙胤立志传.v1.0.0f8.2/LongYinLiZhiZhuan/BepInEx/plugins/LongYinRoster/Slots/slot_01.json', encoding='utf-8-sig')); print(d['_meta']['applySelection'])"
  ```
  → `identity: True` 확인

**기대**: file 즉시 갱신, persistence 정상.

**결과**:

---

## C. v0.3 호환 Apply (V03Default selection)

- [ ] 새 슬롯 캡처 (selection = V03Default — 스탯/명예/천부/스킨 on, 정체성 off)
- [ ] [▼ Apply] 클릭 → 확인 → 진행
- [ ] BepInEx 로그에 Apply 결과 확인:
  ```
  PinpointPatcher.Apply done — applied=N skipped=M warned=X errors=0 fatal=False
  ```
  - 부상/충성/호감 → "selection off" skip 확인 (Category=None)
  - 정체성/자기집 add/무공 active/인벤토리/창고 → "selection off" skip 확인
- [ ] save → reload → 정보창 정상 (G1/G2/G3 회귀 게이트)
- [ ] 적용된 항목: 스탯 + 명예 + 천부 + 스킨

**기대**: v0.3 와 동일 효과. 부상/충성/호감 변경 안 됨 (영구 보존). 정보창 NRE 없음.

**결과**:

---

## D. 부분 Apply — 스탯만

- [ ] 슬롯 1 의 체크박스: 스탯만 on, 다른 8 카테고리 off
- [ ] [▼ Apply]
- [ ] 결과: HP/Mana/Power/baseAttri 변경. 명예/천부/스킨 그대로 (이전 game 상태 유지)

**기대**: 단일 카테고리 부분 적용 정상.

**결과**:

---

## E. 정체성 PoC 실증 (가장 중요 — save→reload 검증)

이건 A2 PoC 의 잔여 risk 검증. **save → reload 후에도 변경값이 살아남는지** 확인.

- [ ] 슬롯의 정체성 toggle on → [▼ Apply]
- [ ] 게임 안에서 캐릭터 이름 변화 확인 (정보창)
- [ ] 게임 save (인-game 메뉴 → 저장)
- [ ] 게임 종료
- [ ] 게임 재실행 → save 로드
- [ ] 정보창 — heroName 이 슬롯 값으로 살아있는가? OR 원본으로 회귀?

**Outcome cases**:
- **PASS**: heroName 살아있음 → IdentityFieldMatrix.Setter path 검증 완료. v0.4 ship 가능.
- **FAIL**: heroName 회귀 → IdentityFieldMatrix path 를 Harmony postfix 로 바꿔야 함. v0.4 holding + R&D 추가.

**결과**:

---

## F. 인벤토리 / 창고 PoC FAIL 시나리오

- [ ] 슬롯 1 detail panel 의 인벤토리 / 창고 체크박스 → 회색 disabled + "(v0.5+ 후보)" suffix 표시 확인
- [ ] 클릭 시도 → 토글 안 됨 (GUI.enabled = false 차단)

**기대**: capability gate 가 UI level 에서 작동.

**결과**:

---

## G. 무공 active PoC FAIL 시나리오

- [ ] 슬롯 1 detail panel 의 무공 active 체크박스 → 회색 disabled + "(v0.5+ 후보)" suffix 표시 확인
- [ ] 클릭 시도 → 토글 안 됨

**결과**:

---

## H. Restore 항상 모두 적용 (체크박스 무관)

- [ ] 임의 슬롯 [▼ Apply] → 자동백업 슬롯 0 생성 확인
- [ ] 슬롯 0 detail panel — 체크박스 노출 안 됨, [↶ 복원] 버튼만
- [ ] [↶ 복원] 클릭 → 확인 → 진행
- [ ] BepInEx 로그 — 9 카테고리 모두 시도 (RestoreAll, 단 capability gate 로 disabled 카테고리는 자동 skip)
- [ ] save → reload → 정보창 정상

**기대**: Apply 직전 상태로 정확히 복귀.

**결과**:

---

## I. PoC 실패 disabled UI

이미 F/G 에서 다룸 — 통합 결과:
- [ ] 무공 active / 인벤토리 / 창고 → 회색 + "(v0.5+ 후보)" suffix
- [ ] 정체성 → enabled (PoC PASS)

**결과**:

---

## J. v0.2 / v0.3 슬롯 호환성 (legacy)

기존 v0.3 슬롯이 있는 경우:
- [ ] v0.3 시기 캡처된 슬롯 (file mtime 확인) — 모드 창에서 정상 표시
- [ ] detail panel — 체크박스 default V03Default 적용 (스탯/명예/천부/스킨 on)
- [ ] 슬롯 file 확인 — `_meta.applySelection` field 추가되지 않음 (read-only fallback, file mtime 변경 없음)
- [ ] 사용자가 toggle 한 번 시도 → 그 시점에 file 갱신 (mtime 업데이트)
- [ ] file 안 `_meta.applySelection` 추가됨 확인

**기대**: 끊김 없이 전이. 자동 마이그레이션.

**결과**:

---

## 회귀 게이트

- [ ] v0.3 의 G1/G2/G3 (save→reload 후 정보창 정상) — 9 카테고리 어떤 조합 후에도 통과
- [ ] 40/40 unit tests pass (이미 빌드 시 검증)

---

## Smoke 종합 결과 (2026-04-30)

- **A** (default): ✅ PASS
- **B** (toggle 저장): ✅ PASS
- **C** (v0.3 호환): ✅ PASS
- **D** (부분 Apply): ✅ PASS
- **E** (정체성 save/reload — 핵심): ✅ PASS
- **F** (인벤토리/창고 disabled): ✅ PASS
- **G** (무공 active disabled): ✅ PASS
- **H** (Restore): ✅ PASS
- **I** (disabled UI): ✅ PASS
- **J** (v0.3 호환): ✅ PASS

**천부 (RebuildHeroTagData)** — 첫 시도 시 FAIL (`InvalidOperationException`). 진단 결과 v0.3 부터 잠재된 schema 가정 오류 발견. Fix commit `eaf2938` (heroTagData 가 Array, entry property 가 tagID/leftTime/sourceHero) 후 재검증 → **17/17 천부 적용 PASS**.

---

## 결론

- [x] **모두 PASS — v0.4 ship ready** (천부 fix 포함). Task D16 (cleanup) 진행.
- [ ] ~~E FAIL~~ — 해당 없음
- [ ] ~~다른 FAIL~~ — 천부만 발견 → fix `eaf2938` 적용 후 PASS

**Build chain**: `8f3edf1` (C14) → `10069fd` (C14 followup) → `eaf2938` (heroTagData fix)
**Tests**: 40/40 PASS through entire chain
**다음**: D16 → D17 → D18.
